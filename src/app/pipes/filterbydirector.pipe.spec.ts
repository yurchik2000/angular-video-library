import { FilterbydirectorPipe } from './filterbydirector.pipe';

describe('FilterbydirectorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbydirectorPipe();
    expect(pipe).toBeTruthy();
  });
});
