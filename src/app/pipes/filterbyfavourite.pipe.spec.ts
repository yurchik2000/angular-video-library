import { FilterbyfavouritePipe } from './filterbyfavourite.pipe';

describe('FilterbyfavouritePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbyfavouritePipe();
    expect(pipe).toBeTruthy();
  });
});
